version: 2.1
orbs:
    node: circleci/node@3.0.1

jobs:
    build:
        executor:
            name: node/default
        steps:
            - checkout
            - node/install-packages
            - run: npm run build

workflows:
    build:
        jobs:
            - build
