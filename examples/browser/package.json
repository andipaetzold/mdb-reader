{
    "name": "mdb-reader-browser",
    "version": "0.0.0",
    "source": "src/index.html",
    "scripts": {
        "dev": "rimraf dist && parcel",
        "build": "rimraf dist && parcel build"
    },
    "dependencies": {
        "buffer": "6.0.3"
    },
    "devDependencies": {
        "parcel": "2.3.1",
        "rimraf": "3.0.2"
    }
}
