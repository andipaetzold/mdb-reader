{
    "name": "mdb-reader-browser",
    "version": "0.0.0",
    "source": "src/index.html",
    "scripts": {
        "dev": "rimraf dist && parcel",
        "build": "rimraf dist && parcel build"
    },
    "dependencies": {
        "buffer": "6.0.3",
        "mdb-reader": "3.0.0"
    },
    "devDependencies": {
        "parcel": "2.10.3",
        "rimraf": "5.0.5"
    }
}
