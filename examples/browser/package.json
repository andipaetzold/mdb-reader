{
    "name": "mdb-reader-browser",
    "version": "0.0.0",
    "source": "src/index.html",
    "scripts": {
        "dev": "rimraf dist && parcel",
        "build": "rimraf dist && parcel build"
    },
    "dependencies": {
        "buffer": "6.0.3",
        "mdb-reader": "2.2.6"
    },
    "devDependencies": {
        "parcel": "2.2.1",
        "rimraf": "4.1.1"
    }
}
